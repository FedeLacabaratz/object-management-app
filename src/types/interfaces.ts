//I create a file to store interfaces. I would place here only the ones I use in multiple files
export interface ObjectType {
    id: string;
    name: string;
    description: string;
    type: string;
}